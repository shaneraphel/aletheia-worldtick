# PEAK dir bind

A PEAK direction is registered only after it names a peak id, so two peak maps cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/peak-dir-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
