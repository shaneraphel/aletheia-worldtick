# OpenCV frame bind

A captured frame is registered only after it names a camera id, so two cameras cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/opencv-frame-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
