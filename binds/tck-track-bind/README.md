# TCK track bind

A TCK track is registered only after it names a tck id, so two tracts cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/tck-track-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
