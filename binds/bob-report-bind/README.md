# Bob report bind

A bob report is registered only after it names a bob id, so two reports cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/bob-report-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
