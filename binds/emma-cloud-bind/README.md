# emma cloud bind

An emma node is registered only after it names a cloud id, so two clouds cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/emma-cloud-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
