# SNIRF meas bind

A SNIRF measurement is registered only after it names a snirf id, so two measurements cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/snirf-meas-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
