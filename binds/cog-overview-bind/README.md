# COG overview bind

A Cloud Optimized GeoTIFF overview is registered only after it names a cog id, so two overviews cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/cog-overview-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
