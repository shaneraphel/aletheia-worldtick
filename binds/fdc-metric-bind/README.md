# FDC metric bind

An FDC metric is registered only after it names an fdc id, so two metrics cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/fdc-metric-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
