# Agent skill bind

A skill is registered only after it names a skill id, so two agents cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/agent-skill-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
