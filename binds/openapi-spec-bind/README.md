# OpenAPI spec bind

An OpenAPI spec is registered only after it names an openapi id, so two specs cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/openapi-spec-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
