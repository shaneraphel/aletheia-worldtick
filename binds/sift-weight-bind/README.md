# SIFT weight bind

A SIFT weight is registered only after it names a sift id, so two weight maps cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/sift-weight-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
