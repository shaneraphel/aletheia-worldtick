# Parquet column bind

A Parquet column is registered only after it names a parquet id, so two columns cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/parquet-column-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
