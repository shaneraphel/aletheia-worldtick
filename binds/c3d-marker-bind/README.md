# C3D marker bind

A C3D marker is registered only after it names a c3d id, so two markers cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/c3d-marker-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
