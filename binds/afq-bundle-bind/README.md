# AFQ bundle bind

An AFQ bundle is registered only after it names an afq id, so two bundles cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/afq-bundle-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
