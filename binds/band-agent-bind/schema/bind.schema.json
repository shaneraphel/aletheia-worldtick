{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "band-agent-bind bind tape",
  "type": "object",
  "required": [
    "band_id",
    "rows"
  ],
  "additionalProperties": false,
  "properties": {
    "band_id": {
      "type": "string",
      "minLength": 1
    },
    "rows": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": [
          "goal"
        ],
        "properties": {
          "goal": {
            "type": [
              "string",
              "integer"
            ]
          }
        }
      }
    }
  }
}
