# Band agent bind

A band agent is registered only after it names a band id, so two bands cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/band-agent-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
