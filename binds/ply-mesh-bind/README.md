# PLY mesh bind

A PLY mesh is registered only after it names a ply id, so two faces cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/ply-mesh-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
