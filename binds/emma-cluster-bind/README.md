# emma cluster bind

A probe is queued only after it names a cluster id, so two clusters cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/emma-cluster-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
