# ORC stripe bind

An ORC stripe is registered only after it names an orc id, so two stripes cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/orc-stripe-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
