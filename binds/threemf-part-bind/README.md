# 3MF part bind

A 3MF part is registered only after it names a threemf id, so two builds cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/threemf-part-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
