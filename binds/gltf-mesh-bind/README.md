# glTF mesh bind

A glTF mesh is registered only after it names a gltf id, so two meshes cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/gltf-mesh-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
