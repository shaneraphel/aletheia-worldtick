# URDF joint bind

A URDF joint is registered only after it names a urdf id, so two joints cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/urdf-joint-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
