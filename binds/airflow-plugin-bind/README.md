# Airflow plugin bind

An Airflow plugin hook is registered only after it names a plugin id, so two plugins cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/airflow-plugin-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
