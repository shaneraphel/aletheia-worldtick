# MINC slice bind

A MINC slice is registered only after it names a minc id, so two slices cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/minc-slice-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
