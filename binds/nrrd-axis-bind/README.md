# NRRD axis bind

A NRRD axis is registered only after it names a nrrd id, so two axes cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/nrrd-axis-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
