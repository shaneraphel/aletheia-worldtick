# TRX group bind

A TRX group is registered only after it names a trx id, so two bundles cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/trx-group-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
