# Avro block bind

An Avro block is registered only after it names an avro id, so two blocks cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/avro-block-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
