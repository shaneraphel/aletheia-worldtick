# TRK stream bind

A TRK stream is registered only after it names a trk id, so two tracts cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/trk-stream-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
