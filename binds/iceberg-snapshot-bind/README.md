# Iceberg snapshot bind

An Iceberg snapshot is registered only after it names an iceberg id, so two snapshots cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/iceberg-snapshot-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
