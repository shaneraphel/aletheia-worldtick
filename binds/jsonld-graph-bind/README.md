# JSON-LD graph bind

A JSON-LD graph is registered only after it names a jsonld id, so two graphs cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/jsonld-graph-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.

## Catalog

Cross-repo dest Pages tape: `fixtures/catalog.json` plus `fixtures/catalog.jsonld`.
Pages copy: https://shaneraphel.github.io/jsonld-graph-bind/catalog.jsonld
