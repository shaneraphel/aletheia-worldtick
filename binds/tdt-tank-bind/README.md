# TDT tank bind

A TDT tank is registered only after it names a tdt id, so two tanks cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/tdt-tank-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
