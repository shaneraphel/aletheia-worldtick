# Arrow batch bind

An Arrow record batch is registered only after it names an arrow id, so two batches cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/arrow-batch-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
