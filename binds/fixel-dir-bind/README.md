# FIXEL dir bind

A FIXEL direction is registered only after it names a fixel id, so two fixels cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/fixel-dir-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
