# BVEC dir bind

A BVEC direction is registered only after it names a bvec id, so two direction sets cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/bvec-dir-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
