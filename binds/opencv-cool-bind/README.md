# OpenCV COOL bind

A COOL tile is registered only after it names a graviton id, so two tiles cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/opencv-cool-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
