# NetCDF var bind

A NetCDF variable is registered only after it names a netcdf id, so two variables cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/netcdf-var-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
