# CONNECTOME edge bind

A connectome edge is registered only after it names a connectome id, so two graphs cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/connectome-edge-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
