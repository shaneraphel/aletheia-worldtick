# LAS point bind

A LAS point is registered only after it names a las id, so two points cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/las-point-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
