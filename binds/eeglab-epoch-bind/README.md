# EEGLAB epoch bind

An EEGLAB epoch is registered only after it names an eeglab id, so two epochs cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/eeglab-epoch-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
