# AMD act bind

An AMD act kernel is registered only after it names an act id, so two kernels cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/amd-act-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
