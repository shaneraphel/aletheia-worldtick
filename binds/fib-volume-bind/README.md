# FIB volume bind

A FIB volume is registered only after it names a fib id, so two fibre volumes cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/fib-volume-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
