# GDF record bind

A GDF header is registered only after it names a gdf id, so two recordings cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/gdf-record-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
