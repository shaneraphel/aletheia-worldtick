# Vultr rush bind

A Vultr rush run is registered only after it names a rush id, so two runs cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/vultr-rush-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
