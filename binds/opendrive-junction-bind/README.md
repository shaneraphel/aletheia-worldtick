# OpenDRIVE junction bind

An OpenDRIVE junction is registered only after it names an xodr id, so two junctions cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/opendrive-junction-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
