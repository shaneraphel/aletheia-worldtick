# DWI series bind

A DWI series is registered only after it names a dwi id, so two series cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/dwi-series-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
