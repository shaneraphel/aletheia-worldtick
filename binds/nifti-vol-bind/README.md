# NIfTI vol bind

A NIfTI volume is registered only after it names a nifti id, so two volumes cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/nifti-vol-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
