# VTK cell bind

A VTK cell is registered only after it names a vtk id, so two meshes cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/vtk-cell-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
