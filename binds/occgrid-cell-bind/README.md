# Occupancy cell bind

An occupancy cell is registered only after it names a grid id, so two cells cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/occgrid-cell-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
