# BIDS sidecar bind

A BIDS sidecar is registered only after it names a bids id, so two sidecars cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/bids-sidecar-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
