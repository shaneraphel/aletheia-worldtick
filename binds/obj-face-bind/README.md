# OBJ face bind

An OBJ face is registered only after it names an obj id, so two meshes cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/obj-face-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
