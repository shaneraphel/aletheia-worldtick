# Alexa intent bind

An Alexa utterance is registered only after it names an intent id, so two intents cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/alexa-intent-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
