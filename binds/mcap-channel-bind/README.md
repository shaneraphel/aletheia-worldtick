# MCAP channel bind

An MCAP channel is registered only after it names an mcap id, so two topics cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/mcap-channel-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
