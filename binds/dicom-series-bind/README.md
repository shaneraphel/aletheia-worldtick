# DICOM series bind

A DICOM SOP instance is registered only after it names a dicom id, so two instances cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/dicom-series-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
