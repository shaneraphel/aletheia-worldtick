# Rosbag topic bind

A rosbag topic is registered only after it names a bag id, so two topics cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/rosbag-topic-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
