# EDF signal bind

An EDF signal is registered only after it names an edf id, so two channels cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/edf-signal-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
