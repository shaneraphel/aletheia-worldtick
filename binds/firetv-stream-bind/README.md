# Fire TV stream bind

A Fire TV stream is queued only after it names a device id, so two devices cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/firetv-stream-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
