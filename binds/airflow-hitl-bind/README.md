# Airflow HITL bind

An Airflow approval is registered only after it names a hitl id, so two approvals cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/airflow-hitl-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
