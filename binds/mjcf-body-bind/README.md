# MJCF body bind

An MJCF body is registered only after it names an mjcf id, so two links cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/mjcf-body-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
