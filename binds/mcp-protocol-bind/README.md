# MCP protocol bind

An Alexa+ style MCP session accepts JSON-RPC 2.0 only after the protocol version is `2025-11-25` and the transport is Streamable HTTP.

## Demo

https://shaneraphel.github.io/mcp-protocol-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
