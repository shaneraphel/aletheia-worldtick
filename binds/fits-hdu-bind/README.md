# FITS HDU bind

A FITS HDU is registered only after it names a fits id, so two extensions cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/fits-hdu-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
