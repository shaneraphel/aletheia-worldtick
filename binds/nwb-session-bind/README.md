# NWB session bind

An NWB session is registered only after it names an nwb id, so two sessions cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/nwb-session-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
