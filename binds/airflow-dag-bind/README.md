# Airflow dag bind

A task is queued only after it names a dag id, so two DAGs cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/airflow-dag-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
