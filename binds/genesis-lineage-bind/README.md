# Genesis lineage bind

A genesis seed is stored only after it names a lineage id, so two lineages cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/genesis-lineage-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
