# Agent week slot bind

An agent-week booking is stored only after it names a week id, so two weeks cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/agentweek-slot-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
