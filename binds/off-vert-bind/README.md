# OFF vert bind

An OFF vertex count is registered only after it names an off id, so two meshes cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/off-vert-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
