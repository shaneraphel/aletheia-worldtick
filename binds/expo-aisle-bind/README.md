# Expo aisle bind

Booth registration requires an aisle so two halls cannot append into one global list.

## Demo

https://shaneraphel.github.io/expo-aisle-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
