# Resource tape bind

A resource tape is registered only after it names a tape id, so two tapes cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/resource-tape-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
