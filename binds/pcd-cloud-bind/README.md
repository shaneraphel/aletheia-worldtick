# PCD cloud bind

A PCD cloud is registered only after it names a pcd id, so two clouds cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/pcd-cloud-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
