# Schema format bind

A format schema is registered only after it names a schema id, so two schemas cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/schema-format-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
