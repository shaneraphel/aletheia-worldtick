# MIF image bind

An MIF image is registered only after it names a mif id, so two images cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/mif-image-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
