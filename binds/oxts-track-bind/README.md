# OXTS track bind

An OXTS track is registered only after it names an oxts id, so two fixes cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/oxts-track-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
