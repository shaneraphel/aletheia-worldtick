# CDS contact bind

A CDS message is registered only after it names a contact id, so two contacts cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/cds-contact-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
