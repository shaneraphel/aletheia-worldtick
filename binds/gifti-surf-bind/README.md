# GIFTI surf bind

A GIFTI surface is registered only after it names a gifti id, so two surfaces cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/gifti-surf-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
