# Zarr array bind

A Zarr array is registered only after it names a zarr id, so two arrays cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/zarr-array-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
