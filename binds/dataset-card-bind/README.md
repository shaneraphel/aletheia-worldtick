# Dataset card bind

A dataset card is registered only after it names a card id, so two cards cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/dataset-card-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
