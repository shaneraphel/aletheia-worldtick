# Bee hive bind

A Bee note is queued only after it names a hive id, so two hives cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/bee-hive-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
