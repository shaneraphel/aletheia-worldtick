# Delta commit bind

A Delta commit is registered only after it names a delta id, so two commits cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/delta-commit-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
