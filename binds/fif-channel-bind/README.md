# FIF channel bind

A FIF channel is registered only after it names a fif id, so two channels cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/fif-channel-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
