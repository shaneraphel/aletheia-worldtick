# NSX chan bind

An NSX channel is registered only after it names an nsx id, so two channels cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/nsx-chan-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
