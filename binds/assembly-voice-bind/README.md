# Assembly voice bind

An assembly clip is registered only after it names a voice id, so two voices cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/assembly-voice-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
