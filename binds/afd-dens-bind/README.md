# AFD dens bind

An AFD density is registered only after it names an afd id, so two densities cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/afd-dens-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
