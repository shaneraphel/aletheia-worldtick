# OpenCV vision bind

An OpenCV vision action is registered only after it names a vision id, so two actions cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/opencv-vision-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
