# Ring doorbell bind

A Ring chime is recorded only after it names a doorbell id, so two doorbells cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/ring-doorbell-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
