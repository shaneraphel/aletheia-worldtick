# Aviation leg bind

An aviation leg is registered only after it names a flight id, so two flights cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/aviation-leg-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
