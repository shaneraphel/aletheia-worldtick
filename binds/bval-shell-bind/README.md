# BVAL shell bind

A BVAL shell is registered only after it names a bval id, so two shells cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/bval-shell-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
