# Sandbox isolate bind

Queued sandbox work requires an isolate id so two sandboxes cannot append into one global list.

## Demo

https://shaneraphel.github.io/sandbox-isolate-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
