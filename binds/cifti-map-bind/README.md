# CIFTI map bind

A CIFTI map is registered only after it names a cifti id, so two maps cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/cifti-map-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
