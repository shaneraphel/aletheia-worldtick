# FOD SH bind

An FOD spherical harmonic is registered only after it names a fod id, so two FODs cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/fod-sh-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
