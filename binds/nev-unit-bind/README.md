# NEV unit bind

A NEV unit is registered only after it names a nev id, so two units cannot append into one unsigned list.

## Demo

https://shaneraphel.github.io/nev-unit-bind/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
