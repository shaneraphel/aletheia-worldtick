# Playable BCI files

This kernel now **ingests** BIDS-EEG channels and events in
`resources/synthetic/` (five channels, sick 0 and 4, four orders).
An empty events table refuses.

```bash
python3.12 show_bids.py
```
