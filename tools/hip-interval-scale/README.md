# HIP interval scale

An interval sample is scaled without the affine shift. A point sample keeps the shift.

## Demo

https://shaneraphel.github.io/hip-interval-scale/

## Resource

Playable tape: `schema/bind.schema.json` plus `fixtures/sample.json`.
A named schema without a fixture is not a resource.
